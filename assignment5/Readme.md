# Fraction

## Organization

`src` - source code

`bin` - binary file

`temp` - build dir

## Quick Start

`run.bat` or `run.bash` and see the test result in the terminal

> `run.sh` is tested, while `run.bat` is not
